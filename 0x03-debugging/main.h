#include <stdio.h>
#ifndef MAIN_H
#define MAIN_H
/**
 * Main - entry point
 * Return: Always return 0;
 */
int _putchar(char c);
void positive_or_negative(int i);
int largest_number(int a, int b, int c);

#endif /* MAIN.H */
