#include "main.h"
/**
 * int _islower(int c) - prototype function .
 *
 * main - check the code.
 *
 * Return: Always 0.
 */
int _islower(int c)
{
	if (_islower(c) >= 'a' && _islower(c) <= 'z')
	{
		return (1);
	}
	else
		return (0);
	return (0);
}

