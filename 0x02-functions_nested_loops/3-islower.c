#include "main.h"
#include <stdio.h>
/**
 * int _islower(int c) - prototype function .
 *
 * main - check the code.
 *
 * Return: Always 0.
 */
int main(void){
	int _islower(int c);

	if (_islower(int c) >= 'a' && _islower(int c) <= 'z')
	{
		return (1);
	}
	else
		return (0);
	return (0);

}

